repos:
  - repo: local
    hooks:
      - id: ktlint-format
        name: ktlint format
        language: system
        entry: bash -c 'cd backend && ./gradlew ktlintFormat'
        files: \.kt$
        pass_filenames: false
